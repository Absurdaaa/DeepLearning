"""随机种子、设备与 matplotlib 环境占位。"""

from __future__ import annotations

from pathlib import Path


def setup_matplotlib(project_root: Path) -> None:
    """下一步在这里设置 matplotlib 缓存目录。"""

    _ = project_root


def set_seed(seed: int) -> None:
    """下一步在这里统一随机种子。"""

    _ = seed
